# Core runtime
torch==2.7.0      # GPU: add '+cu118' or '+cu126' wheels
tensorflow==2.19.0
# Optional optimisation
cython==3.1.2     # only install when you need it
# Dev / tooling
black
ruff
pytest
mypy